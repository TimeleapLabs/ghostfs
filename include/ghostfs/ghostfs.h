#pragma once

#include <string>

namespace ghostfs {

  class GhostFS {
  public:
    /**
     * @brief Creates a new ghostfs
     * @param name the name to greet
     */
    GhostFS();
  };

}  // namespace ghostfs
